// Re-export from features layer (single source of truth) to keep the FSD entity barrel valid
export { mockProperties, mockPropertyDetails } from "@/features/properties/data";
export { mockPropertyDetails as propertyDetails } from "@/features/properties/data";
